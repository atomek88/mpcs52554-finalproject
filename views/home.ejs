<!DOCTYPE html>
<html>

    <head>
        <% include ./partials/head %>
    </head>
        <body class="container">
          <header>
          <% include ./partials/navbar %>
        </header>

        <div class="jumbotron">
          <h1> <%= user.username %>'s Home page!</h1>

      </div>
      <div class="container">
        <ul class="list-group">
  <li class="list-group-item">User Information below</li>
  <li class="list-group-item">YOUR API KEY: <%= user.api_key %></li>
  <li class="list-group-item">ID:<%= user.id %></li>
</ul>
      <form method="post" action="/api/notifications">
        <div class="form-group">
          <input type="hidden" name="id" id="id" value="{{this._id}}">
          <label for="item">Add Notifications Url</label>
          <input type="text" class="form-control" id="url" name="url">
        </div>
        <button type="submit" class="btn btn-default">Add</button>
      </form>
      <form action="users/{{user.id}}" method="post">
        <input type="hidden" name="item" id="item" value="{{this._id}}">
        <div class="form-group">
      <button type="submit" class="btn btn-danger">Delete Account here</a>
      </div>
    </form>
  </div>

</body>
</html>
